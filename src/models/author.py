class Author():
    def __init__(self, _id: int, name: str, collaborations: int, works: int):
        self._id = _id
        self.name = name
        self.collaborations = collaborations
        self.works = works
